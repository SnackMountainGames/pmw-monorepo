# game-command-center

## Building

Run `nx build game-command-center` to build the app.

## Running Locally

Run `nx dev game-command-center` to run the app locally.

## Deploying

This project uses [Vercel](https://vercel.com/), a cloud platform that makes it easy to deploy
web applications very easily.