# websocket-lambda

## Building

Run `nx build websocket-lambda` to build the app. 

## Deploying

Run `nx deploy websocket-lambda` to deploy the app directly to AWS Lambda.
This requires an AWS CLI connection in your console. Instructions for that
will probably be added later.